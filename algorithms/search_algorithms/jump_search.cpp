#include <iostream>
#include <map>
#include <bits/stdc++.h>

using namespace std;

class Solution{
    public:
        jump_search(int arr[], int x, int arr_size){
            int step = sqrt(arr_size);
            int prev = 0;
            while (arr[min()]){
                
            }
            
        }
}